# Direct dependencies — human-editable source of truth.
# Dockerfile installs from requirements.lock (exact pins) for reproducible builds.
# To upgrade: edit bounds here → docker compose build backend → pip freeze > requirements.lock

# Web framework
fastapi>=0.115,<1
uvicorn>=0.30,<1

# Database
sqlalchemy[asyncio]>=2.0,<3
psycopg2-binary>=2.9,<3
asyncpg>=0.29,<1
alembic>=1.13,<2

# Pydantic / settings
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Auth
python-jose[cryptography]>=3.3,<4
bcrypt>=4.0,<6

# LLM SDKs — pinned tightly because upstream breaking changes are common
openai>=2.0,<3
anthropic>=0.100,<1
google-genai>=2.8,<3

# LangChain (concierge orchestration)
langchain>=1.0,<2

# Maps / HTTP
httpx>=0.27,<1
google-auth>=2.30,<3

# Async / background tasks
celery>=5.4,<6
redis>=5.0,<9

# Utilities
python-dotenv>=1.0,<2
python-dateutil>=2.9,<3
cachetools>=5.0,<8
email-validator>=2.1,<3

# Payments / notifications
razorpay>=2.0,<3
app-store-server-library>=3.0,<4
resend>=2.0,<3

# Testing
pytest>=8.0,<10
pytest-asyncio>=0.23,<2
aiosqlite>=0.20,<1

# API spec tooling (dev/CI only)
pyyaml>=6.0,<7
openapi-spec-validator>=0.7,<1
openapi-core>=0.14,<1
schemathesis>=4.0,<5
datamodel-code-generator>=0.25,<1
